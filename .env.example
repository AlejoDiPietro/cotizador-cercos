# Cadena de conexión de PostgreSQL.
#
# En Neon conviene la URL "pooled" (la que dice -pooler): en serverless cada
# request puede caer en una instancia distinta y sin pooler se agotan las
# conexiones de la base antes que la paciencia.
DATABASE_URL="postgresql://usuario:clave@host/base?sslmode=require"
